# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Temporary files
*.tmp
*.log

# Secrets directory - ignore actual secret files but allow .example files
secrets/**/*.yaml
secrets/**/*.key
secrets/**/*.pem
secrets/**/*.crt
secrets/**/*.csr
secrets/**/*.secret.yaml
!secrets/**/*.example
!secrets/**/README.md

# Other secret files
*.secret.yaml
*.key
*.pem
*.crt
*.csr
certs/
# Wazuh password file
wazuh-passwords.txt
# Local environment files
.env
.env.local
# Harbor secret files with actual passwords
harbor/base/harbor-secrets.yaml
harbor/base/harbor-secret-patch.yaml
# Wazuh secret files - the template files in wazuh/base/ are safe (commented placeholders only)
# Only exclude if someone creates actual secret files with real values
# The template files (*-secret.yaml) in wazuh/base/ are intentionally committed as they contain no secrets
# Note: harbor-helmchartconfig.yaml is now safe (no passwords) and is committed# HelmChartConfig should NOT be committed - it contains the token
# HelmChartConfig files contain tokens and should not be committed
**/*helmchartconfig.yaml
